# Local development uses data/game-state.json when these variables are empty.
# On Vercel, connect an Upstash Redis database and set these env vars.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: use a separate namespace for previews, staging, or another party.
KNOWBETTER_STORAGE_PREFIX=knowbetter
